version: "3.8"

services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    command: npm start -- --host 0.0.0.0 --port 3000 --cache /usr/src/app/cache
    environment:
      - NODE_ENV=development